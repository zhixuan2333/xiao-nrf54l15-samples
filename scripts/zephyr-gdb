#!/usr/bin/env bash
# Wrapper so VS Code (cortex-debug) can find the Zephyr GDB regardless of its
# PATH: source the toolchain env, then exec arm-zephyr-eabi-gdb.
source /opt/toolchain-env.sh 2>/dev/null || true
exec arm-zephyr-eabi-gdb "$@"
